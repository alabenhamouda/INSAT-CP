{% extends 'problem/layout.html.twig' %}

{% block main %}
<table class="table table-hover">
        <thead>
        <tr>
                <th scope="col">Rang</th>
                <th scope="col">User</th>
                {% for problem in problems %}
                <th scope="col"> <a href="{{ path('problem', {id: id, letter: problem.letter}) }}"> {{ problem.letter }} </a> </th>
                {% endfor %}

        </tr>
        </thead>
        <tbody>
        {% for problem in problems %}
                <tr>
                        <td>
                                1
                        </td>
                        <td>
                               User1
                        </td>
                        {% for problem in problems %}
                                <td scope="col" class="align-center" > + <br> 1:15 </td>
                        {% endfor %}
                </tr>
        {% endfor %}
        </tbody>
</table>
{% endblock %}
